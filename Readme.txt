Graduate Project


1.설정파일은 xml->/target/classes/applicationContext.xml 파일로 우선 사용.
2.commit방법 : 폴더 생성 -> repository로 사용하기 -> 설정에 이메일 등록 -> 수정 후 commit
3.push방법 : 로컬에서 수정 후에, pull -> commit -> push. 단, 다른사람이 먼저 push 했을 경우 다시 pull해서 수정사항을 체크-> 충돌해결-> 다시 push.
4.인태는 adRemover 구현부 확인할 것.
